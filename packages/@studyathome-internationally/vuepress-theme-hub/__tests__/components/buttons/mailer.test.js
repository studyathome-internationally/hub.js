// import Vue from "vue";
import Mailer from "../../../components/buttons/Mailer.vue";

describe("Mailer", () => {
  // Inspect the raw component options
  it("has a property name", () => {
    expect(typeof Mailer.name).toBe("string");
  });
});
